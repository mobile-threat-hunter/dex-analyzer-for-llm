{
  "permissions": {
    "allow": [
      "Bash(*)",
      "WebFetch(domain:github.com)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "/home/nyahumi/Project/Dexkit/.claude/dad-reminder.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -qE '(/vendor/dexkit_core/Core/|/new_dexkit/binding/).*\\.(cpp|cc|h|hpp)$' && echo '\u26a0 DexKit C++ source modified \u2014 run /dexkit-build before testing or sweeping. The currently-installed .so is now stale.' || true"
          }
        ]
      }
    ]
  }
}