{
  "permissions": {
    "allow": [
      "Bash(*)",
      "WebFetch(domain:github.com)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "/home/nyahumi/Project/Dexkit/.claude/dad-reminder.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -qE '(/dexkit_git/Core/|/new_dexkit/binding/).*\\.(cpp|cc|h|hpp)$' && echo '⚠ DexKit C++ source modified — run /dexkit-build before testing or sweeping. The currently-installed .so is now stale.' || true"
          }
        ]
      }
    ]
  }
}
